# Redirector domains REFUSED at create time (the `shorteners` feed seed).
# One domain per line; lines starting with # are comments.
#
# This list is deliberately NARROW: it holds only redirector services whose
# practical use on spoo.me has been evasion — every one below appeared in a
# live abuse campaign, wrapping a phishing destination so the stored URL
# looked clean. Refusing them costs no legitimate traffic.
#
# Mainstream shorteners (bit.ly, t.co, lnkd.in, tinyurl.com, buff.ly ...)
# are deliberately NOT here. People legitimately share tweet and LinkedIn
# links, and blocking those sacrifices real traffic for no safety gain --
# the abuse rides the obscure services above, not the platforms. Links
# through mainstream shorteners are allowed and judged by what their chain
# actually ends at (redirect resolution), not by the wrapper.
#
# Entries seed the feed ONCE, on first boot with an empty feed. After that
# the DB owns the set: operators (and the deep-analysis tier, for services
# it classifies as abuse-only) add entries live, and removals are never
# re-added from this file.

# cloak redirectors observed wrapping abuse on spoo.me
l24.im
mub.me
n9.cl
e.vg
bitly.cx
vlk.by
goo.su
x.gd
